use bevy::prelude::*;

fn main() {
    App::new()
        // Plugins
        .add_plugins(DefaultPlugins)
        //.add_plugin(bevy_inspector_egui::quick::WorldInspectorPlugin::new())
        
        // Resources

        // Startup systems

        // Update systems

        // Run
        .run();
}